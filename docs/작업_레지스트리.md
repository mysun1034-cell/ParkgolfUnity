# 파크골프 — 작업 레지스트리 (현재 상태 단일 진실)

> 규칙: **append만**(덮어쓰기·삭제 금지, 정정도 옛 행 보존+정정행). 결함·작업을 행으로 추가하고 닫을 때 ✅+커밋ID.
> "됐나/남았나"는 이 표 + 코드(grep)로 확인. ROADMAP·서사·요약은 검증 대상이지 출처 아님.
> ID 규칙: `T-###`=작업(task), `B-###`=버그(defect).

| ID | 구분 | 항목 | 상태 | 커밋 | 비고 |
|----|------|------|------|------|------|
| T-001 | 작업 | 설계문서 + ParkGolf 드롭인 패키지(Runtime/Editor/asmdef/README/샘플) 초기 등록 | ✅ | `30957ef` | 적대적 코드리뷰 통과(compile likely·high 0). Unity 미설치라 실제 컴파일 미검증 |
| T-002 | 작업 | 자산 위생 — Unity `.gitignore` + `.gitattributes`(LFS) + git-lfs 활성화 | ✅ | `a1edb39` | check-ignore/check-attr 게이트 GREEN. `.anim`은 텍스트 유지 |
| T-003 | 작업 | 작업 규율 이식 §1 — 문서 세트(CLAUDE.md 정본 + docs/ 스켈레톤) | ✅ | (본 커밋) | CareLink 규율을 Unity/게임으로 적응. 도메인특화 제외 |

## 미해결 / 다음 (상세는 `docs/ROADMAP.md`)
| ID | 항목 | 상태 |
|----|------|------|
| T-004 | `ParkGolf.Core` asmdef 분리(순수로직 → EditMode 테스트) | 미착수 |
| T-005 | `scripts/check.ps1`(Unity 감지·미설치 SKIPPED 정직표기) | 미착수 |
| T-006 | git 훅(commit-msg: Co-Authored-By 차단+trailer / pre-commit: check; core.hooksPath, **LFS 훅 우회 주의**) | 미착수 |
| T-007 | CI(game-ci/unity-test-runner) | 보류(Unity 미설치·라이선스/비용 미정) |
| T-008 | Unity 설치·프로젝트 생성(버전·RP 확정) → 실제 컴파일·렌더 검증 | 미착수(외부 의존) |
